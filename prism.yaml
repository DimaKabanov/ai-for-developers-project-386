# Prism Configuration for API Mocking
# Based on TypeSpec contract

# Server configuration
server:
  port: 3000
  host: localhost

# CORS settings
cors: true

# Enable dynamic mocking
dynamic: true

# Path to the OpenAPI specification
spec: ./tsp-output/openapi.yaml
